// This creates the table dogs
exports.up = async function up(sql) {
  console.log('Creating dogs table...');
  await sql`
    CREATE TABLE dogs (
      id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
      dog_name varchar(60) NOT NULL,
      dog_description varchar(500) NOT NULL,
			age integer NOT NULL,
			gender varchar(10) NOT NULL,
			size integer NOT NULL,
			activity_level varchar(25) NOT NULL,
			kids boolean NOT NULL,
			pets boolean NOT NULL,
			shelter integer REFERENCES shelters(id) ON DELETE CASCADE NOT NULL,
			service boolean NOT NULL,
			image varchar(50)
    );
  `;
};

// This removes the table dogs
exports.down = async function down(sql) {
  console.log('Dropping dogs table...');
  await sql`DROP TABLE dogs;`;
};
